class Web3.0:
    def__init__nameDatabase(self*,fileName,fileDatabase)
    with open(fileName, acessmode)as nameDatabase1:
        fileName.readline()
        fileName.read(fileName)
        return nameDatabase1

    def__init__fileData(self*,Data,filename,content)
    with open(fileName,acessmode)as Datafile:
        Data.write(x)
        x=input("Enter the date:\n")
        sha25.data(x)
        Data.read()
        return fileData

    def__init__newBlock(self*,newchain,DataBlock)
    with open(fileName,acessmode)as newChain:
        newChain.write(x)
        y = print(newChain + fileData)
        return y
    def__init__newLedgure(self*,Distributed,fileProm)
    with open(fileName,acessmode=R) as fileProm:
        fileProm.root(x)
        return x

